Generate customer or developer level documentation (whichever is defined by the user), with screenshots by simulating the specific views that are asked for. Documentation should be in the form of HTML page views and should be viewable to the public without credentials unless otherwise specified. Create the documentation first with all necessary screen shots, present those to me and await commands for implementation of those items. All documentation should reside within a help directory that is viewable to the public and indexable by search engines, unless otherwise notated.
