# Release 1.3.0 — Next.js 16 upgrade

**Branch:** `chore/next-16` (from `dev` + dependency consolidation)

---

## Summary

- Upgrade **Next.js 15 → 16** and **eslint-config-next 16**
- Rename **`middleware.ts` → `proxy.ts`** (Next 16 convention)
- Migrate **Sass** config to `loadPaths` (required for Turbopack + sass-loader 16)
- Migrate **ESLint** to native flat config (`eslint-config-next/core-web-vitals`)
- Turbopack is now the default bundler for `next dev` and `next build`

## Test plan

- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run lint`
- [x] `npm run lint:styles`
- [x] `npm run format:check`
- [x] `npm run build`
- [ ] Smoke tenant/customer nav + auth on `lvh.me`
- [ ] Verify subdomain rewrites (admin / my / tenant slug) still work via `proxy.ts`

## Follow-up

- Re-enable `react-hooks/set-state-in-effect` and `react-hooks/purity` and fix ~27 call sites
- Stripe SDK 22 migration (separate PR)
